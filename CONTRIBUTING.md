# Contributing

Read `AGENTS.md`, `docs/AI_PROJECT_BRIEF.md`, and `docs/TEST_PLAN.md` before changing code.

Use a focused branch, keep commits small, update tests and documentation together, and run `./scripts/check.sh` plus `git diff --check` before every commit. Never weaken the active-brush requirement or report unperformed interactive tests as passing.
